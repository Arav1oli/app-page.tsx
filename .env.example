DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Gmail send) — Cloud Console > APIs & Services > Credentials > OAuth client ID (Web application)
# Authorized redirect URI: ${NEXTAUTH_URL}/api/auth/callback/google
# OAuth consent screen scopes must include: https://www.googleapis.com/auth/gmail.send
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
